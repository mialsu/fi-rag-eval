"""fi-rag-eval — Finnish waste-regulation QA whose evaluation harness is the product.

Nothing is implemented yet; see DESIGN.md for the milestones. This package exists so the
build gate (`uv sync --locked` + import) has something real to prove.
"""

__version__ = "0.0.0"
