# SPEC — <task>

Written after `/grill-with-docs`, before any code, on the Owner's agreement. Records decisions
with their *why*, not just the what. Kept as a thinking artifact (in `specs/`), never inside a
build output.

## Problem
What we're solving, and for whom. One paragraph.

## Solution shape
The decided approach — data, logic, surface, words — at the altitude of "what", not line-level
"how". Reference `CONTEXT.md` vocabulary.

## Tracer slices
The end-to-end vertical slices, blockers first. Each is demoable alone.
1. <slice> — <the thin full-stack cut it delivers>
2. <slice> (blocked by 1)

## Non-goals
What this deliberately does NOT do. Scope creep dies here.
- <non-goal>

## Open questions (for the Owner / customer)
Ambiguities that need a human answer — not resolved by assumption.
- <question>

## Decisions
Load-bearing or reversed decisions get an ADR (link it here).
- <decision> → ADR-NNNN
